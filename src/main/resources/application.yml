spring:
  datasource:
   url: jdbc:postgresql://localhost:5432/test
#    url: jdbc:postgresql://postgresqldb:5432/test
    username: alex
    password: alex
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    generate-ddl: true
    database: postgresql

