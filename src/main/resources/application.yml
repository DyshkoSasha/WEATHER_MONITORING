spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    password: alex
    username: alex
    url: jdbc:postgresql://localhost:5432/test
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    generate-ddl: true
    database: postgresql

